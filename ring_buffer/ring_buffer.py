class RingBuffer:
    def __init__(self, value, capacity=6):
        self.head = value
        self.tail = value
        self.full = capacity
        self.length = capacity if capacitty is not 0

    def append(self, item):
        # current node
        current = self.head
        new = item

        if new:
            new = self.head

        return self.full

    def get(self):
        all_values = self.full

        if all_values:

        return self.full
